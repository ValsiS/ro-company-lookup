# Artisan command

```bash
php artisan ro-company-lookup:check 123456 --date=2024-01-10 --raw
```

## Options

- `--date=YYYY-MM-DD`: optional query date
- `--raw`: include `meta.raw` in output

The command outputs JSON to stdout.
