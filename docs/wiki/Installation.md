# Installation

## Requirements

- PHP 8.3+
- Laravel 12+

## Install

```bash
composer require valsis/ro-company-lookup
```

## Publish config

```bash
php artisan vendor:publish --tag=ro-company-lookup-config
```

The config file will be available at `config/ro-company-lookup.php`.
